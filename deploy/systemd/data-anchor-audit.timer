[Unit]
Description=每日锚定审计链尾发飞书运维群留痕（删尾即与昨日锚点对不上→不可抵赖）
[Timer]
# OnCalendar 按服务器 OS 本地时区 Asia/Shanghai(CST)。02:07 CST = 印尼业务日次日凌晨，
# 晚于各 sync/聚合 timer，锚定当日已沉淀完整的链尾。
OnCalendar=*-*-* 02:07:00
Persistent=true
[Install]
WantedBy=timers.target
