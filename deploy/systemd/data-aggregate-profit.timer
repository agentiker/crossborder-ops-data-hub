[Unit]
Description=每日聚合各店前一日预估利润（写 fact_profit_daily，供利润卡）
[Timer]
# OnCalendar 按 OS 本地时区。生产机 OS=Asia/Jakarta(WIB)，01:33 = 印尼当地，晚于 unsettled
# 采集(01:13)；聚合昨日完整数据，印尼早晨即可看到昨日预估利润。(hp 测试机 OS=CST 早 1h，无碍。)
OnCalendar=*-*-* 01:33:00
Persistent=true
[Install]
WantedBy=timers.target
